---
export interface Props {
  text?: string;
  speed?: number;
}
const { text = "OPEN TO WORK", speed = 10 } = Astro.props;
---

<div class="marquee-container border-b py-4 font-light text-xs bg-accent-yellow text-black">
  <div class="marquee" data-speed={speed}>
    <span class="marquee-content">{text}</span>
    <span class="marquee-content">{text}</span>
    <span class="marquee-content">{text}</span>
    <span class="marquee-content">{text}</span>
    <span class="marquee-content">{text}</span>
    <span class="marquee-content">{text}</span>
    <span class="marquee-content">{text}</span>
    <span class="marquee-content">{text}</span>
    <span class="marquee-content">{text}</span>
    <span class="marquee-content">{text}</span>
    <span class="marquee-content">{text}</span>
    <span class="marquee-content">{text}</span>
    <span class="marquee-content">{text}</span>
    <span class="marquee-content">{text}</span>
    <span class="marquee-content">{text}</span>
    <span class="marquee-content">{text}</span>
    <span class="marquee-content">{text}</span>
    <span class="marquee-content">{text}</span>
    <span class="marquee-content">{text}</span>
    <span class="marquee-content">{text}</span>
    <span class="marquee-content">{text}</span>
    <span class="marquee-content">{text}</span>
    <span class="marquee-content">{text}</span>
    <span class="marquee-content">{text}</span>
    <span class="marquee-content">{text}</span>
    <span class="marquee-content">{text}</span>
    <span class="marquee-content">{text}</span>
    <span class="marquee-content">{text}</span>                                
  </div>
</div>

<style>
  .marquee-container {
    width: 100%;
    overflow: hidden;
  }
  
  .marquee {
    display: flex;
    width: fit-content;
    gap: 2rem;
    will-change: transform;
  }
  
  .marquee-content {
    white-space: nowrap;
    line-height: 1;
    animation: scroll linear infinite;
  }
  
  @keyframes scroll {
    0% {
      transform: translateX(0);
    }
    100% {
      transform: translateX(calc(-100% - 2rem));
    }
  }

  /* Infinite horizontal motion is exactly what WCAG 2.2.2 and reduced-motion
     preferences are about, so hold it still and show the text in place. */
  @media (prefers-reduced-motion: reduce) {
    .marquee-content {
      animation: none;
    }

    .marquee {
      overflow-x: auto;
    }
  }
</style>

<script>
  document.addEventListener('astro:page-load', () => {
    const marquees = document.querySelectorAll('.marquee');
    
    marquees.forEach((marquee) => {
      const speed = parseFloat(marquee.getAttribute('data-speed') || '100');
      const firstContent = marquee.querySelector('.marquee-content');
      
      if (firstContent) {
        const contentWidth = firstContent.offsetWidth;
        const duration = contentWidth / speed;
        
        marquee.querySelectorAll('.marquee-content').forEach((content) => {
          (content as HTMLElement).style.animationDuration = `${duration}s`;
        });
      }
    });
  });
</script>