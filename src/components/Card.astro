---
import CardLabel from "./Card-label.astro";

interface Props {
  size: string;
}

const { size } = Astro.props;
---

<div
  class={size === "small"
    ? "bg-white hover:bg-blue px-12 py-3 group w-full relative transition ease-in-out hover:cursor-pointer"
    : "bg-white hover:bg-blue px-12 py-3 group w-full col-span-2 row-span-2 relative transition ease-in-out hover:cursor-pointer"}
>
  <div class="flex flex-col justify-between min-h-full">
    <slot />
  </div>

  <style>
    img {
      width: 100%;
    }
  </style>
</div>
