---
const currentPath = new URL(Astro.request.url).pathname;

interface Props {
  href: string;
  name: string;
}

const { href, name } = Astro.props;
---

<a href={href}>
  <span
    class={currentPath === href
      ? "underline lg:first:ml-0"
      : "lg:first:ml-0 hover:text-blue"}
  >
    {name}
  </span>
</a>
