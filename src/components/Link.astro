---
const currentPath = new URL(Astro.request.url).pathname;

interface Props {
  href: string;
  name: string;
  footer?: boolean;
}

const { href, name, footer = false } = Astro.props;
---

<a class={footer ? "underline" : "underline hover:text-blue"} href={href}
  >{name}</a
>
