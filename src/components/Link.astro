---
const currentPath = new URL(Astro.request.url).pathname;

interface Props {
  href: string;
  name: string;
}

const { href, name } = Astro.props;
---

<a class="underline hover:text-blue hover:font-bold" href={href}>{name}</a>
