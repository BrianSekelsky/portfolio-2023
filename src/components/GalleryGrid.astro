---
import cardMap from './card-map.js';
---

<div
  id="gallery"
  class="gallery grid grid-cols-1 md:grid-cols-2 lg:grid-cols-6 gap-4 px-4 sm:px-8 py-6"
>
  {cardMap.map(({ Component, typeClass }) => (
    <Component typeClass={typeClass} />
  ))}
</div>