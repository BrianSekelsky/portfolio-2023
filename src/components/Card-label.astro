---
interface Props {
  isLarge: boolean;
  value: string;
}

const { isLarge, value } = Astro.props;
---

<p
  class={isLarge
    ? "uppercase text-lg font-mono font-bold group-hover:capitalize"
    : "uppercase font-bold font-mono mb-0 group-hover:lowercase"}
>
  {value}
</p>
