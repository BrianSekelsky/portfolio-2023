---
interface Props {
  isLarge: boolean;
  value: string;
}

const { isLarge, value } = Astro.props;
---

<p
  class={isLarge
    ? "text-xl uppercase font-mono"
    : "text-xs uppercase font-mono"}
>
  {value}
</p>
