---
interface Props {
  isLarge: boolean;
  value: string;
}

const { isLarge, value } = Astro.props;
---

<p class={isLarge ? "text-2xl font-mono" : "text-xs uppercase font-mono mb-0"}>
  {value}
</p>
